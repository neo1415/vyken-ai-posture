# Database seeds

## `seed.ts`

Seeds **tool categories only** in Module 3. No AI vendor profile claims, no leads, no assessments.

```bash
pnpm db:seed
```

Requires `DATABASE_URL` and applied migrations.

## Module 4

Tool identity records and curated profile versions will be seeded in Module 4 with reviewed confidence levels. Do not invent vendor privacy, security, or training-use claims here.
