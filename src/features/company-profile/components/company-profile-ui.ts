/** Shared Tailwind/class maps for company profile Vyken UI. */
export const vykenOptionCardClasses =
  "vyken-card-interactive group relative flex cursor-pointer items-start gap-3 p-4 has-[:checked]:vyken-selected-card has-[:focus-visible]:outline has-[:focus-visible]:outline-2 has-[:focus-visible]:outline-offset-2 has-[:focus-visible]:outline-[var(--focus-ring)]";

export const vykenCheckboxCardClasses =
  "vyken-card-interactive group relative flex cursor-pointer items-start gap-3 p-4 has-[:checked]:vyken-selected-card has-[:focus-visible]:outline has-[:focus-visible]:outline-2 has-[:focus-visible]:outline-offset-2 has-[:focus-visible]:outline-[var(--focus-ring)] data-[disabled=true]:cursor-not-allowed data-[disabled=true]:opacity-50";

export const vykenNativeControlClasses =
  "border-border text-primary mt-0.5 size-4 shrink-0 accent-[var(--primary)]";
